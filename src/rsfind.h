#ifndef RSFIND_H_
#define RSFIND_H_
///@file
/**in-depth recursive search starting from filePath
stores files to process in global variable filesFound
*/
void recursiveSearch(char* filePath,char* fileName);

#endif