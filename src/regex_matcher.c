#include <stdio.h>
#include <stdbool.h>
#include "structures.h"


bool regexNameMatch(char * path){
	printf("%s\n%s\n",myArgs.ename,path);
	return true;
}

bool regexTextMatch(char* path){
	printf("%s\n%s\n",myArgs.T,path);
	return true;
}